{
  "name": "browser-game",
  "version": "1.0.0",
  "description": "ブラウザゲーム",
  "main": "public/ModalWindow.js",
  "scripts": {
    "test": "jest",
    "build": "tsc",
    "serve": "http-server ./public -c-1",
    "start": "npm run build && npm run serve"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {},
  "devDependencies": {
    "http-server": "^14.1.1",
    "typescript": "^5.8.3",
    "jest": "^30.0.5",
    "ts-jest": "^29.4.0",
    "@types/jest": "^30.0.0"
  }
}